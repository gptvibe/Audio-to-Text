<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="App_Desktop.Pages.ModelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="28" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock Text="Models" Style="{StaticResource SectionTitleTextBlockStyle}" />
            <TextBlock Text="Download Whisper-compatible models from Hugging Face and keep them in local app data." Style="{StaticResource MutedTextBlockStyle}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="CustomRepoTextBox" Header="Advanced repo ID" PlaceholderText="org/model-name" />
                <Button Grid.Column="1" Content="Add and Download" Click="DownloadCustomRepo_Click" VerticalAlignment="Bottom" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2">
            <StackPanel x:Name="ModelsStackPanel" Spacing="12" />
        </ScrollViewer>
    </Grid>
</Page>
