<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="App_Desktop.Controls.HistorySidebar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>
    </UserControl.Resources>

    <Grid Padding="12" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button
            x:Name="NewTranscriptionButton"
            Style="{StaticResource AccentButtonStyle}"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Left"
            Click="NewTranscription_Click">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <FontIcon Glyph="&#xE710;" FontSize="16" />
                <TextBlock Text="New Transcription" />
            </StackPanel>
        </Button>

        <TextBox
            x:Name="SearchBox"
            Grid.Row="1"
            PlaceholderText="Search transcripts"
            TextChanged="SearchBox_TextChanged" />

        <Grid Grid.Row="2">
            <ListView
                x:Name="HistoryListView"
                IsItemClickEnabled="True"
                ItemClick="HistoryListView_ItemClick"
                SelectionMode="Single">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Margin" Value="0,2" />
                        <Setter Property="CornerRadius" Value="8" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10,8" RowSpacing="3">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Text="{Binding SourceName}"
                                FontWeight="SemiBold"
                                TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                Grid.Row="1"
                                Text="{Binding Details}"
                                FontSize="12"
                                Style="{StaticResource MutedTextBlockStyle}"
                                TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock
                x:Name="EmptyStateTextBlock"
                Margin="10,16"
                Style="{StaticResource MutedTextBlockStyle}"
                Text="No transcripts yet."
                Visibility="Collapsed" />
        </Grid>

        <StackPanel Grid.Row="3" Spacing="4">
            <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,2,0,6" />

            <Button Style="{StaticResource SidebarButtonStyle}" Tag="live" Click="Navigation_Click">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <FontIcon Glyph="&#xE720;" FontSize="16" />
                    <TextBlock Text="Live Transcription" />
                </StackPanel>
            </Button>

            <Button Style="{StaticResource SidebarButtonStyle}" Tag="models" Click="Navigation_Click">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <FontIcon Glyph="&#xE8B7;" FontSize="16" />
                    <TextBlock Text="Models" />
                </StackPanel>
            </Button>

            <Button Style="{StaticResource SidebarButtonStyle}" Tag="settings" Click="Navigation_Click">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <FontIcon Glyph="&#xE713;" FontSize="16" />
                    <TextBlock Text="Settings" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
